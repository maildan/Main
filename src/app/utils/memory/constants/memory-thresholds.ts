/**
 * 메모리 임계값 정의 (실무에서 권장하는 단계별 대응)
 */
export const MEMORY_THRESHOLDS = {
  LOW: 50 * 1024 * 1024,       // 50MB - 주의 단계
  MEDIUM: 100 * 1024 * 1024,   // 100MB - 경고 단계
  HIGH: 150 * 1024 * 1024,     // 150MB - 위험 단계
  CRITICAL: 200 * 1024 * 1024  // 200MB - 긴급 단계
};

/**
 * 최적화 수준에 따른 설명
 */
export const OPTIMIZATION_LEVEL_DESCRIPTIONS = {
  0: '정상 단계 (최적화 필요 없음)',
  0.5: '관찰 단계 (경량 최적화)',
  1: '주의 단계 (중간 수준 최적화)',
  2: '경고 단계 (고수준 최적화)', 
  3: '위험 단계 (최대 수준 최적화)'
};
